name: Build

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/API_KEY

      - name: Access API key variable
        run: |
          echo "API Key is ${{ vars.API_KEY }}"
